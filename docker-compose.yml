services:
  postgres:
    # pgvector/pgvector:pg16 = PostgreSQL 16 + pgvector 扩展(RAG 向量检索用)
    # 基于官方 PG16 构建,数据目录布局一致,可直接复用已有的 postgres_data 卷
    image: pgvector/pgvector:pg16
    container_name: ai_cs_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ai_cs
      POSTGRES_PASSWORD: ai_cs_pwd
      POSTGRES_DB: ai_customer_service
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ai_cs -d ai_customer_service"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: ai_cs_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  postgres_data:
  redis_data:
